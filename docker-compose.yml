services:
  mysql:
    image: mysql:8.4
    environment:
      MYSQL_DATABASE: local_tour_planner
      MYSQL_USER: planner
      MYSQL_PASSWORD: planner
      MYSQL_ROOT_PASSWORD: root
    ports: ["3306:3306"]
    volumes: ["mysql-data:/var/lib/mysql"]
volumes:
  mysql-data:
